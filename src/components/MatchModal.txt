import React from "react";

export default function MatchModal({ host, onClose }) {
  return (
    <div style={overlay} onClick={onClose}>
      <div style={card}>
        <h1 style={{ fontSize: 28 }}>💜 Connection Formed</h1>

        <p style={{ color: "#cbd5e1" }}>
          You and {host?.alias || host?.name} are now connected
        </p>

        <button onClick={onClose} style={btn}>
          Continue
        </button>
      </div>
    </div>
  );
}

const overlay = {
  position: "fixed",
  inset: 0,
  background: "rgba(0,0,0,0.85)",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  zIndex: 99999,
};

const card = {
  background: "#0f172a",
  color: "#fff",
  padding: 30,
  borderRadius: 16,
  textAlign: "center",
  maxWidth: 360,
};

const btn = {
  marginTop: 15,
  padding: "10px 14px",
  borderRadius: 10,
  border: "none",
  background: "#7c3aed",
  color: "#fff",
  fontWeight: 700,
  cursor: "pointer",
};